'use server'

/**
 * @deprecated Import directly from `@/service/library` instead.
 * Thin re-export wrappers kept for backward compatibility.
 */

export { save, create, remove, archive, unarchive, unstar } from '@/service/library'
