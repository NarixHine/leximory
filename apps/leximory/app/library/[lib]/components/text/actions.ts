'use server'

/**
 * @deprecated Import directly from `@/service/text` instead.
 * Thin re-export wrappers kept for backward compatibility.
 */

export { addText as add, addAndGenerateText as addAndGenerate, getVisitedTextsAction as getVisitedTexts } from '@/service/text'
